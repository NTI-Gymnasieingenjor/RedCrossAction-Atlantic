<!DOCTYPE html>
<html lang="sv">
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0"/>
        <title>Röda Korset - Login - Jour</title>

        <!-- CSS  -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link href="css/style.css" type="text/css" rel="stylesheet" media="screen,projection"/>
    </head>
    <body>
        <div class="container">
            <nav style="background: #FFF; box-shadow: none;">
                <div class="nav-wrapper">
                    <a href="#" class="brand-logo" style="top: 10px">
                        <img src="./images/logo.svg" style="height: 40px" /> 
                    </a>
                </div>
            </nav>
        </div>

        <div class="container">
            <h2 class="red-text">Dashboard - Jour</h2>
            <div class="row">
                <div class="col s12 l6">
                    <div class="card-panel white">
                        <h4>Logga in</h4>
                        <% if(typeof data !== "undefined"){ %>
                            <p class="red-text"><%- data.error_msg %></p>
                        <% } %>
                        <form method="POST", action="/auth">
                            <div class="row">
                                <div class="input-field col s12 m10">
                                    <input name="username" id="username" type="text" placeholder="Användarnamn" class="validate">
                                    <label for="username">Användarnamn</label>
                                </div>
                            </div>
                            <div class="row">
                                <div class="input-field col s12 m10">
                                    <input name="password" id="password" type="password" placeholder="Lösenord" class="validate">
                                    <label for="password">Lösenord</label>
                                </div>
                            </div>
                            <div class="row">
                                <button class="btn red white-text waves-effect waves-light" type="submit">
                                    Logga in
                                    <i class="material-icons right">lock</i>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <!--  Scripts-->
        <script src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/chart.js@2.8.0"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
        <script src="js/init.js"></script>

    </body>
</html>
